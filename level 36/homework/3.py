# The map function applies a specified function to each item in an iterable
# It returns an iterator with the results of the function

# Takes two arguments: the function and the iterable

# If multiple iterables are provided, the function is applied in parallel to the items
